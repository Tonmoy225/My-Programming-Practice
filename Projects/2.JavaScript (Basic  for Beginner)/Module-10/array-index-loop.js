const numbers = [47,54,53,64,76];

const length = numbers.length;

for( let i = 0 ; i < numbers.length ; i++){
    console.log('value of index :', i );

    const items = numbers[i];
    console.log('value of the item is ', items);
}